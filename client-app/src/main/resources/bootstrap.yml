spring:
  application:
    name: config-client-app
  profiles:
    active: dev
  cloud:
    config:
      discovery:
        enabled: true
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka